DEVICE_ID=pi-001
DEVICE_SECRET=

# ── API server ────────────────────────────────────────────────────────────────
API_BASE_URL=https://your-api-server.com
API_UPLOAD_PATH=/scans
API_HEARTBEAT_PATH=/devices/heartbeat
API_TIMEOUT_SECONDS=30

# ── Local Flask settings ──────────────────────────────────────────────────────
FLASK_PORT=5055
IMAGE_DIR=./data/images
LOG_DIR=/tmp/logs

# Set to this Pi's LAN IP so the API server can POST grade results back here
DEVICE_HOST=192.168.1.100

# ── Mode switch ───────────────────────────────────────────────────────────────
# production: upload scans to API backend for grading
# training:   upload captures to Roboflow
EDGE_MODE=production
PRODUCTION_UPLOAD_TARGET=api
TRAINING_UPLOAD_TARGET=roboflow

# ── Roboflow (training mode only) ────────────────────────────────────────────
ROBOFLOW_API_KEY=
ROBOFLOW_WORKSPACE=
ROBOFLOW_PROJECT=
ROBOFLOW_DATASET_NAME=edge-captures

# ── Worker behaviour ──────────────────────────────────────────────────────────
HEARTBEAT_INTERVAL_SECONDS=60
UPLOADER_POLL_SECONDS=3
UPLOADER_MAX_RETRIES=5
